apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agora-server-chart.fullname" . }}-test-connection"
  labels:
    {{- include "agora-server-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agora-server-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
